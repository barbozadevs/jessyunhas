const menuBtn=document.querySelector('.menu-btn'),navLinks=document.querySelector('.nav-links');if(menuBtn)menuBtn.addEventListener('click',()=>navLinks.classList.toggle('open'));document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')));
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const glow=document.querySelector('.cursor-glow');window.addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});
const key='jessy_reviews_v1';let reviews=JSON.parse(localStorage.getItem(key)||'[]');const list=document.getElementById('reviewList'),avg=document.getElementById('avgRating'),stars=document.getElementById('avgStars'),count=document.getElementById('reviewCount');
function renderReviews(){if(!reviews.length){list.innerHTML='<div class="empty-reviews">Ainda não há avaliações. Seja a primeira cliente a deixar sua opinião. ♡</div>';}else{list.innerHTML=reviews.slice().reverse().map(r=>`<article class="review"><div class="review-top"><span class="review-name">${escapeHtml(r.name)}</span><span class="review-date">${r.date}</span></div><div class="review-stars">${'★'.repeat(r.stars)}${'☆'.repeat(5-r.stars)}</div><p>${escapeHtml(r.text)}</p></article>`).join('')}const total=reviews.reduce((s,r)=>s+r.stars,0),mean=reviews.length?total/reviews.length:0;avg.textContent=mean.toFixed(1);stars.textContent='★'.repeat(Math.round(mean))+'☆'.repeat(5-Math.round(mean));count.textContent=`${reviews.length} ${reviews.length===1?'avaliação':'avaliações'}`}
function escapeHtml(s){return s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}renderReviews();
let selected=5;document.querySelectorAll('#starPicker button').forEach(btn=>btn.addEventListener('click',()=>{selected=Number(btn.dataset.star);document.getElementById('reviewStars').value=selected;document.querySelectorAll('#starPicker button').forEach(b=>b.classList.toggle('active',Number(b.dataset.star)<=selected))}));document.querySelectorAll('#starPicker button').forEach(b=>b.classList.add('active'));
document.getElementById('reviewForm').addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('reviewName').value.trim(),text=document.getElementById('reviewText').value.trim();if(!name||!text)return;reviews.push({name,text,stars:selected,date:new Date().toLocaleDateString('pt-BR')});localStorage.setItem(key,JSON.stringify(reviews));e.target.reset();selected=5;document.querySelectorAll('#starPicker button').forEach(b=>b.classList.add('active'));renderReviews();document.getElementById('avaliacoes').scrollIntoView({behavior:'smooth'})});
